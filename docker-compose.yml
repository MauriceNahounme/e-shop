version: "3.6"

services:
  frontoffice:
    build:
      context: ./frontoffice
      dockerfile: Dockerfile
    container_name: frontoffice
    restart: always
    networks:
      - ecommerce-network



networks:
  ecommerce-network:
    driver: bridge